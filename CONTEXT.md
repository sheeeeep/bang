# Skill 管理

## Language

**个人库**：集中保存用户挑选的 skill 的唯一内容来源；存入个人库本身不代表在项目中启用。

**全局入口**：手动或由模型下载安装 skill 后、归集前的暂存位置；其中的 skill 可能被 agent 全局加载。

**安装**：按来源渠道将完整 skill 放入全局入口，等待用户归集；不代表已经存入个人库或在项目启用。

**个人 skill**：用户自行维护并分发的 skill 版本；是来源分类，不等于存放所有来源 skill 的个人库。

**项目选择**：一个项目希望启用的个人库 skill 集合；再次选择得到的新集合取代旧集合。搜索过滤只改变可见项，不改变这个集合，隐藏的已选 skill 仍属于项目选择。

**归集**：将全局入口中的 skill 搬入个人库，并移除其全局入口副本的操作。同名归集视为候选升级，不代表已验证版本更新。
