# Skill 管理

## Language

**个人库**：集中保存用户挑选的 skill 的唯一内容来源；存入个人库本身不代表在项目中启用。

**全局入口**：手动下载或复制 skill 后、归集前的暂存位置；其中的 skill 可能被 agent 全局加载。

**项目选择**：一个项目希望启用的个人库 skill 集合；再次选择得到的新集合取代旧集合。

**归集**：将全局入口中的 skill 搬入个人库，并移除其全局入口副本的操作。同名归集视为候选升级，不代表已验证版本更新。
