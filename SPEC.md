# Skill 管理 CLI：最小可用规格

## 目标

自动化个人本机的两条流程：为项目选择并链接 skill；将手动下载或复制到全局入口的 skill 归集到个人库。

本轮仅确定规格，不实现 CLI。需求已收敛，规模适合单次实现，不建立 wayfinder 决策地图。领域术语见 [CONTEXT.md](./CONTEXT.md)。

## 范围与目录

- 个人库：`~/my-skills`（当前为 `/Users/bytedance/my-skills`）。
- 全局入口：仅 `~/.agents/skills`。
- 项目链接位置：项目根目录下的 `.agents/skills/`。
- 以目录名作为 skill 的同名识别依据；仅识别含 `SKILL.md` 的真实 skill 目录。
- 不对接安装器，不维护安装器升级记录，不自动判断版本新旧。

## 命令一：项目选择

1. 列出个人库中的 skill，提供交互式多选。
2. 预选项目中已指向个人库的 skill 链接。
3. 本次选择是期望的完整集合，不是只追加：勾选项创建链接，取消勾选项移除已有的个人库链接。
4. 移除仅作用于项目链接，绝不删除个人库内容。
5. 项目中的真实目录、文件或指向其他位置的链接不得覆盖；冲突项报告并跳过。
6. 修改前展示新增、移除、冲突及 Git 排除变更，确认后执行；取消不修改。
7. 重复运行且选择不变时，不重复创建链接或排除规则。

### Git 本地排除

- 使用 Git 本地 `info/exclude`，不创建或修改 `.gitignore`。
- 只排除本命令管理的具体链接，不笼统忽略整个 `.agents/`，避免隐藏项目自己的内容。
- 不重复添加排除规则，不覆盖用户已有规则。
- 已跟踪路径不能靠 exclude 取消跟踪；报告并跳过，不自动执行取消跟踪操作。

## 命令二：归集

1. 扫描全局入口中的真实 skill 目录；来源软链接跳过并报告。
2. 展示待搬入项和同名冲突，在修改前确认。
3. 无同名冲突时，将整个目录搬入个人库。
4. 同名时必须明确确认；先保留旧版本备份，再完整替换，不能合并目录而残留旧文件。
5. 未确认的同名项保留原状并跳过。
6. 成功后移除原全局入口副本，不留下指向个人库的软链接，即不再通过该入口全局启用。
7. 提醒引用旧位置的链接可能失效，不擅自修改其他 agent 的目录。已知本机存在 `~/.claude/skills` 指向全局入口的链接。
8. 操作失败时保留可恢复副本，不提前删除唯一有效来源；报告失败项、实际保留位置及备份位置。
9. 同名升级保持个人库原路径，使项目现有链接无需重建即可使用替换后的内容。

## 安全约束

- 只修改本流程明确管理的目标，不删除无关文件或 skill。
- 备份不能被当成可选 skill，也不能因后续升级覆盖此前备份。
- 预览和实际写入之间若发生目标冲突，不得静默覆盖。
- 本规格不承诺批量操作全部成功或全部回滚；必须逐项报告结果，失败后可安全重试。

## 最小验收场景

- 首次选择两个 skill：生成两个有效链接，Git 本地排除生效，`.gitignore` 不变。
- 再次运行：预选正确；取消一个只删除其项目链接，个人库原目录不变。
- 项目存在同名真实目录或外部链接：保持原状并报告冲突。
- 项目存在已跟踪的同名路径：保持原状，不修改 Git 索引。
- 归集新 skill：个人库内容完整，全局原目录消失，未留下全局软链接。
- 同名升级：取消时无修改；确认后旧内容可从备份恢复，新内容不残留旧文件，原项目链接仍有效。
- 来源为软链接：跳过，不沿链接移动或删除目标。
- 搬移或替换失败：仍有完整可恢复副本，输出能说明哪些项成功、失败或跳过。
- 预览阶段取消：文件系统与 Git 配置均不变。

## 实现阶段的轻量细节

命令名称、实现语言、交互库、备份命名由实现阶段选取最简单的可行方案，不是当前需求阻塞项。项目根目录定位、非 Git 项目的报错方式需在实现时明确并写入使用说明；不得猜测后修改错误的项目。

## 不在本版范围

- 自动下载、安装或查询远端更新。
- 扫描并归集多个 agent 的全局目录。
- 自动修复其他 agent 中的失效链接。
- 安装器锁文件同步、版本比较和依赖解析。
- 通用分发平台、配置体系、备份管理专用命令。
