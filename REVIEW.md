# 实现审查

## bang 改造验收

- 入口改为 `bang init` 与 `bang skill select/collect`；可从本地 Git 克隆通过 `uv tool install .` 安装，无运行时依赖。
- 配置位于仓库外，校验目录、JSON 格式与来源/个人库重叠；确认后原子写入，保留旧排除标记及恢复目录格式。
- 保留工作区原有的非 Git 项目选择改动与回归测试。
- `python3 -B -m unittest -v`：34 项通过（包含真实 PTY 测试）；Ruff 与独立 Pyright 检查通过。
- 临时虚拟环境安装后，`bang --help`、临时 HOME 下的 `bang init` 和 `bang skill collect` 冒烟验证通过。
- 配置读取失败会报告配置文件位置，入口统一返回退出码；无效 JSON 的不写入行为有回归验证。

以下为改名前的历史审查记录。

基线：`d8ee8e0`（已确认规格）；首次实现：`51cec0d`。Standards 与 Spec 由两个独立只读子代理并行审查，修复后另做 Spec 复审。以下记录发现及处置，不替代验收测试。

## Standards

无明确的仓库规范违规；以下四项为结构判断，不是硬性错误：

- `select_skills` 同时处理交互、规划和执行，建议拆分规划。暂不引入额外计划结构，保留当前单命令直线流程。
- `collect_skills` 混合交互与单项恢复协议。已拆出 `collect_one` 和 `restore_old`，将单项失败恢复放在一起。
- 排除规则转义与展示字符串可共享格式化函数。暂不处理：展示的是人读路径，不是可直接写入 exclude 的规则，二者职责不同。
- README 使用当前机器绝对路径，可移植性较弱。保留：目标是当前个人流程，文档也提供可选命令链接方式；迁移时替换示例路径。

## Spec

已修复三个可复现问题，并加入 CLI 边界回归测试：

1. **排除规则先于链接修改**：创建或移除链接失败会使排除规则与实际状态不符。现按实际成功项更新规则，失败移除不取消原排除。
2. **归集中断漏报恢复路径**：复制、发布、清理过程中 Ctrl-C 原先只输出取消。现报告当前项、来源、目标、恢复目录及隐藏待清理目录，退出码为 130，停止后续项。
3. **隐藏名称被一概忽略**：复审发现含 `SKILL.md` 的点号名称目录未被列出。现支持这类 skill，仅排除工具保留的备份／清理目录。

额外验证并修复：用户在 exclude 区块之后追加的否定规则不能悄悄使管理规则失效；管理区块放在保留的用户规则之后。如果更高优先级 `.gitignore` 阻止排除，命令明确报错，不擅自改它。

已知边界：审查指出“拒绝软链接管理根目录／祖先”比规格的固定路径约定更严格。保留为写入安全限制并在 README 明示；本机已检查的个人库与项目符合该布局。未扩展到重定向目录支持。

**汇总：Standards 4 项结构建议（1 项采纳、3 项暂不采纳）；Spec 3 项行为问题已修复，1 项布局限制保留并公开。复审未发现其他可复现的数据丢失风险。**

## 最终验证

- `python3 -B -m unittest -v`：16 项通过，覆盖真实 Git/worktree、取消、冲突、升级、复制／发布／清理失败和中断。
- 独立 Pyright 项目检查：0 错误；Ruff 检查通过；`git diff --check` 通过。
- 辅助 LSP 仍返回 `test_skillctl.py` 中 `import skillctl` 无法解析的过期／错误根目录诊断。已标为误报：同目录模块实际存在，独立 Pyright 与测试均成功导入。未通过删测试或屏蔽源码类型检查来消除该提示。

## 键盘选择迭代 — Standards

基线 `f2ef364`，首版提交 `2aeea00`。两个只读子 Agent 并行完成 Standards／Spec 审查；另两个子 Agent 分别负责 PTY 测试与 README。开发阶段仅运行 focused tests。

审查发现与处置：

- **CSI 方向键误作 Esc**：已补充常见 CSI 编码的解码，保留 curses 原生 terminfo／SS3 支持；增加真实 PTY 回归测试。
- **测试只覆盖应用模式方向键**：现同时覆盖普通模式 CSI 与应用模式 SS3，并明确验证向上和向下移动。
- **历史终端输出可能使中间断言提前满足**（结构建议）：保留输出流作为同步提示，不引入终端模拟器；正确性由后续预览与真实文件状态断言保证。发送前先排空旧输出，Esc 清空测试增加清空后的实际选择验证。
- **未使用初始终端属性**（结构建议）：恢复测试改为与捕获的初始属性完整比较。
- **渲染与按键逻辑混合**（结构建议）：增加 `terminal_key`，集中处理必要的编码兼容，不再扩展状态抽象。

## 键盘选择迭代 — Spec

- **Unicode 等价名称未匹配**：名称和搜索词在 casefold 后做 NFC 规范化，补充组合字符回归测试；该测试同时发现 macOS Git 路径预组合问题，排除规则现遵循仓库的 `core.precomposeunicode` 设置。
- **极小窗口吞掉按键**：少于 5 行或 20 列时明确报错退出，恢复终端且不执行任何项目变更；已补回归测试及使用说明。

**本轮汇总：Standards 2 项行为／覆盖问题已修复、3 项结构建议已处置；Spec 2 项问题已修复。两轴最严重问题分别为方向键误取消、Unicode 名称无法搜索，均有回归测试。额外复审子 Agent 超时且未返回报告，不计为审查通过。**

最终里程碑：将本次暂存内容导出到隔离目录，排除另一组未提交的 `collect` 改动后，全量 **27 项测试通过**（16 项原验收、11 项真实 PTY 验收）；独立 Pyright 0 错误、Ruff 通过。全量测试仅在该最终里程碑执行。
